{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the overlay window",
  "windows": ["overlay", "control", "settings", "howitworks", "dim_layer", "drift_layer"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-close",
    "core:webview:default",
    "opener:default"
  ]
}
